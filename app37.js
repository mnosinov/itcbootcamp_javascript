const calculateArea = (length, width) => `The area is ${length * width}`, myCalc = () => 77;

let length = 3, width = 4;

let area = calculateArea(length, width);
console.log(area);
console.log(myCalc());

area = calculateArea(5, 6);
console.log(area);

area = calculateArea(7, 8);
console.log(area);
